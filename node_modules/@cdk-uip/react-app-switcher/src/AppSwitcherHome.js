import React from "react";
import classnames from "classnames";
import { Icon } from "@cdk-uip/react-icon";

/**
 * Renders a caption with a back icon and text. Used to return
 * to a home page or portal landing page.
 */
const AppSwitcherHome = ({ className, children, ...other }) => (
  <div
    className={classnames(
      "cdk-global-header__app-switcher-back-home-container",
      className
    )}
  >
    <a
      className={classnames(
        "mdc-list-item",
        "cdk-global-header__app-switcher-back-home-anchor"
      )}
      {...other}
    >
      <Icon
        className={classnames(
          "mdc-list-item__graphic",
          "cdk-global-header__app-switcher-back-home-icon"
        )}
      >
        chevron_left
      </Icon>
      <span className="cdk-global-header__app-switcher-back-home-text">
        {children}
      </span>
    </a>
  </div>
);

export default AppSwitcherHome;
