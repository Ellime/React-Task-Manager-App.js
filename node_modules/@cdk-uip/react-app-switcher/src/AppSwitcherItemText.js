import React from "react";
import classnames from "classnames";

/**
 * The label text for an application icon.
 */
const AppSwitcherItemText = ({ className, ...other }) => (
  <span
    className={classnames(
      "cdk-global-header__app-switcher-apps-text",
      className
    )}
    {...other}
  />
);

export default AppSwitcherItemText;
