import React from "react";
import classnames from "classnames";

/**
 * Represents a grid of application icons. Use this to render
 * `<AppSwitchItem>` components.
 */
const AppSwitcherItems = ({ className, children }) => (
  <div
    className={classnames("cdk-global-header__app-switcher-apps", className)}
  >
    <ul
      className="mdc-menu__items mdc-list cdk-global-header__app-switcher-apps-list"
      role="menu"
      aria-hidden="true"
    >
      {children}
    </ul>
  </div>
);

export default AppSwitcherItems;
