import React from "react";
import classnames from "classnames";

/**
 * Used for displaying icons, images or a single letter of text.
 */
const Avatar = ({ className, children, ...other }) => (
  <div className={classnames("cdk-avatar", className)} {...other}>
    {children}
  </div>
);

export default Avatar;
