import React from "react";
import { Badge } from ".";
import renderer from "react-test-renderer";

it("renders", () => {
  expect(
    renderer.create(<Badge className="custom">123</Badge>)
  ).toMatchSnapshot();
});

it("renders disabled", () => {
  expect(renderer.create(<Badge disabled>123</Badge>)).toMatchSnapshot();
});
