import React from "react";
import CardActionButton from "./CardActionButton";

/**
 * Simple wrapper for `CardActionButton` for apps built using
 * a previous version of the component library.
 */
const CardAction = props => <CardActionButton {...props} />;

export default CardAction;
