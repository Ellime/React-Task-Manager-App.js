import React from "react";
import classnames from "classnames";

/**
 * Smaller title used in a `CardHeader` component.
 */
const CardSubtitle = ({ className, ...other }) => (
  <div className={classnames("mdc-card__subtitle", className)} {...other} />
);

export default CardSubtitle;
