import React from "react";
import classnames from "classnames";

/**
 * Display a check mark inside a chip. Use this to create **filter chips**.
 */
const ChipCheckmark = ({ className, ...other }) => (
  <div className={classnames("mdc-chip__checkmark", className)} {...other}>
    <svg className="mdc-chip__checkmark-svg" viewBox="-2 -3 30 30">
      <path
        className="mdc-chip__checkmark-path"
        fill="none"
        stroke="black"
        d="M1.73,12.91 8.1,19.28 22.79,4.59"
      />
    </svg>
  </div>
);

export default ChipCheckmark;
