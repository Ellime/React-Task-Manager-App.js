import React from "react";
import classnames from "classnames";
import { Icon } from "@cdk-uip/react-icon";

/**
 * Used to create leading and trailing chip icons.
 */
const ChipIcon = ({ className, ...other }) => (
  <Icon className={classnames("mdc-chip__icon", className)} {...other} />
);

export default ChipIcon;
