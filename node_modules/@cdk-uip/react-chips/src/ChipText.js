import React from "react";
import classnames from "classnames";

/**
 * The text of a chip.
 */
const ChipText = ({ className, ...other }) => (
  <div className={classnames("mdc-chip__text", className)} {...other} />
);

export default ChipText;
