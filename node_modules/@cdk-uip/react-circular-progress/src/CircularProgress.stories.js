import React from "react";
import { CircularProgress } from ".";
import { storiesOf } from "@storybook/react";

storiesOf("circular-progress", module).add("primary", () => (
  <CircularProgress />
));
