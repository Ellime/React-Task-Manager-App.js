import React from "react";
import { CircularProgress } from ".";
import renderer from "react-test-renderer";

it("renders", () => {
  expect(renderer.create(<CircularProgress />)).toMatchSnapshot();
});

it("renders using a custom class", () => {
  expect(
    renderer.create(<CircularProgress className="custom" />)
  ).toMatchSnapshot();
});
