import React from "react";
import classnames from "classnames";

/**
 * Used to wrap an `Icon` used to navigate your data table.
 */
const DataTablePaginationAction = ({ className, children, ...other }) => (
  <div className={classnames("cdk-data-table-pagination__action", className)}>
    {children}
  </div>
);

export default DataTablePaginationAction;
