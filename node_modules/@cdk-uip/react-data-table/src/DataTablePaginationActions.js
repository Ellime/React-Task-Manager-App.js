import React from "react";
import classnames from "classnames";

/**
 * Used to wrap your `DataTablePaginationAction` components.
 */
const DataTablePaginationActions = ({ className, children, ...other }) => (
  <div className={classnames("cdk-data-table-pagination__actions", className)}>
    {children}
  </div>
);

export default DataTablePaginationActions;
