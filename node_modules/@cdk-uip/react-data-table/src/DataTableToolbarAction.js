import React from "react";
import classnames from "classnames";
import { FluidItem } from "@cdk-uip/react-fluid";

const DataTableToolbarAction = ({ className, children, ...other }) => (
  <FluidItem
    className={classnames("cdk-data-table__toolbar__action", className)}
    {...other}
  >
    {children}
  </FluidItem>
);

export default DataTableToolbarAction;
