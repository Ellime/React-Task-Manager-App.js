import React from "react";
import { Fluid } from "@cdk-uip/react-fluid";

const DataTableToolbarActions = ({ children, ...other }) => (
  <Fluid halign="right" {...other}>
    {children}
  </Fluid>
);

export default DataTableToolbarActions;
