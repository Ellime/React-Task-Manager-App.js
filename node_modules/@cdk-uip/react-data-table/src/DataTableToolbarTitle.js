import React from "react";
import classnames from "classnames";
import { FluidItem } from "@cdk-uip/react-fluid";

const DataTableToolbarTitle = ({ className, children, ...other }) => (
  <FluidItem
    fill
    className={classnames("cdk-data-table__toolbar__title", className)}
  >
    {children}
  </FluidItem>
);

export default DataTableToolbarTitle;
