import React from "react";
import classnames from "classnames";

const DataTableToolbarTitleAlternate = ({ className, children, ...other }) => (
  <span
    className={classnames(
      "cdk-data-table__toolbar__title--alternate",
      className
    )}
  >
    {children}
  </span>
);

export default DataTableToolbarTitleAlternate;
