import React from "react";
import classnames from "classnames";

const DataTableToolbarTitlePrimary = ({ className, children, ...other }) => (
  <span
    className={classnames("cdk-data-table__toolbar__title--primary", className)}
  >
    {children}
  </span>
);

export default DataTableToolbarTitlePrimary;
