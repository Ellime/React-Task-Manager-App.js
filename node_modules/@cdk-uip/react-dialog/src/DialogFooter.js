import React from "react";
import classnames from "classnames";

/**
 * Used to hold `Dialog` footer content, usually `DialogFooterButton` components.
 */
const DialogFooter = ({ className, children, ...other }) => (
  <footer className={classnames("mdc-dialog__footer", className)} {...other}>
    {children}
  </footer>
);

export default DialogFooter;
