import React from "react";
import { NavList } from "@cdk-uip/react-list";

/**
 * Used to create a navigation list.
 */
const DrawerList = ({ children, ...other }) => (
  <NavList {...other}>{children}</NavList>
);

export default DrawerList;
