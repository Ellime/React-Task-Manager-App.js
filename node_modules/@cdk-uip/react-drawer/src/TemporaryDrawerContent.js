import React from "react";
import classnames from "classnames";

/**
 * Often used to contain a `DrawerList` component.
 */
const TemporaryDrawerContent = ({ className, children, ...other }) => (
  <nav
    className={classnames("mdc-drawer__content", "mdc-list-group", className)}
    {...other}
  >
    {children}
  </nav>
);

export default TemporaryDrawerContent;
