import React from "react";
import classnames from "classnames";

/**
 * Used to host `TemporaryDrawerHeaderContent`.
 */
const TemporaryDrawerHeader = ({ className, children, ...other }) => (
  <header className={classnames("mdc-drawer__header", className)} {...other}>
    {children}
  </header>
);

export default TemporaryDrawerHeader;
