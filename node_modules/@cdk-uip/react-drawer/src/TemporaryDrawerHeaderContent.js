import React from "react";
import classnames from "classnames";

/**
 * The drawer header content often has an image as the background.
 */
const TemporaryDrawerHeaderContent = ({ className, children, ...other }) => (
  <div
    className={classnames("mdc-drawer__header-content", className)}
    {...other}
  >
    {children}
  </div>
);

export default TemporaryDrawerHeaderContent;
