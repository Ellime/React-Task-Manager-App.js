import React from "react";
import classnames from "classnames";

/**
 * Use for the content of an expansion panel.
 */
const ExpansionPanelContentBody = ({ className, children, ...other }) => (
  <div className={classnames("content-body", className)} {...other}>
    {children}
  </div>
);

export default ExpansionPanelContentBody;
