import React from "react";
import { CDKLogo } from ".";
import renderer from "react-test-renderer";

it("renders with white text", () => {
  expect(renderer.create(<CDKLogo />)).toMatchSnapshot();
});
it("renders with black text", () => {
  expect(renderer.create(<CDKLogo blackText />)).toMatchSnapshot();
});
