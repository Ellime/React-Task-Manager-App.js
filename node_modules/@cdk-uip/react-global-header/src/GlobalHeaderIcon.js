import React from "react";
import { ToolbarIcon } from "@cdk-uip/react-toolbar";

/**
 * CDK Global Header icon.
 */
const GlobalHeaderIcon = props => <ToolbarIcon {...props} />;

export default GlobalHeaderIcon;
