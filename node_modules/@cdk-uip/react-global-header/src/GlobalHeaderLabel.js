import React from "react";
import classnames from "classnames";

/**
 * A container for the CDK brand and app name.
 */
const GlobalHeaderLabel = ({ className, ...other }) => (
  <div
    className={classnames("cdk-global-header__label", className)}
    {...other}
  />
);

export default GlobalHeaderLabel;
