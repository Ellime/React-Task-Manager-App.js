import React from "react";
import classnames from "classnames";

/**
 * Used to render the brand name in the CDK global header.
 */
const GlobalHeaderLabelBrand = ({ className, ...other }) => (
  <span
    className={classnames("cdk-global-header__label-brand", className)}
    {...other}
  />
);

export default GlobalHeaderLabelBrand;
