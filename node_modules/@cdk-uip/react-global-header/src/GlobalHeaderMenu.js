import React from "react";
import classnames from "classnames";

/**
 * The menu that appers on smaller screens.
 */
const GlobalHeaderMenu = ({ className, ...other }) => (
  <div
    className={classnames("cdk-global-header__menu", className)}
    {...other}
  />
);

export default GlobalHeaderMenu;
