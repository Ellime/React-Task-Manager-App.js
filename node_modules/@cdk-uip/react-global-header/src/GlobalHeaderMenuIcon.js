import React from "react";
import { ToolbarIcon } from "@cdk-uip/react-toolbar";

/**
 * The menu icon ("hamburger") used on smaller screens.
 */
const GlobalHeaderMenuIcon = ({ className, ...other }) => (
  <ToolbarIcon menu className="cdk-global-header__menu-icon" {...other}>
    menu
  </ToolbarIcon>
);

export default GlobalHeaderMenuIcon;
