import React from "react";
import { ToolbarSection } from "@cdk-uip/react-toolbar";

/**
 * Used to divide the CDK global header into sections.
 */
const GlobalHeaderSection = props => <ToolbarSection {...props} />;

export default GlobalHeaderSection;
