import React from "react";
import classnames from "classnames";

/**
 * Used to contain `GridTile` components.
 */
const GridListTiles = ({ className, children, ...other }) => (
  <ul className={classnames("mdc-grid-list__tiles", className)} {...other}>
    {children}
  </ul>
);

export default GridListTiles;
