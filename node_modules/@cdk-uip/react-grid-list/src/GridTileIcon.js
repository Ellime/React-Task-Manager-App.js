import React from "react";
import classnames from "classnames";
import { Icon } from "@cdk-uip/react-icon";

/**
 * Icon displayed within a `GridTileSecondary` conponent.
 */
const GridTileIcon = ({ className, children, ...other }) => (
  <Icon className={classnames("mdc-grid-tile__icon", className)} {...other}>
    {children}
  </Icon>
);

export default GridTileIcon;
