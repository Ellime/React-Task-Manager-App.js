import React from "react";
import classnames from "classnames";

/**
 * Secondary grid tile text.
 */
const GridTileSupportText = ({ className, children, ...other }) => (
  <span
    className={classnames("mdc-grid-tile__support-text", className)}
    {...other}
  >
    {children}
  </span>
);

export default GridTileSupportText;
