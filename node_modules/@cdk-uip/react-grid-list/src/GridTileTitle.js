import React from "react";
import classnames from "classnames";

/**
 * Grid tile text.
 */
const GridTileTitle = ({ className, children, ...other }) => (
  <span className={classnames("mdc-grid-tile__title", className)} {...other}>
    {children}
  </span>
);

export default GridTileTitle;
