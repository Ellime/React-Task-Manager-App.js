import React from "react";
import { IconButton } from ".";
import renderer from "react-test-renderer";

it("renders", () => {
  expect(
    renderer.create(<IconButton className="custom">favorite</IconButton>)
  ).toMatchSnapshot();
});
