import React from "react";
import classnames from "classnames";

/**
 * Indicates the text label in each item, if the Image List contains text labels.
 */
const ImageListLabel = ({ className, ...other }) => (
  <span className={classnames("mdc-image-list__label", className)} {...other} />
);

export default ImageListLabel;
