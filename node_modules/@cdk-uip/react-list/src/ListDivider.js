import React from "react";
import { Divider } from "@cdk-uip/react-divider";

/**
 * Used to separate items in a non-interactive list.
 */
const ListDivider = props => (
  <Divider element="li" role="separator" {...props} />
);

export default ListDivider;
