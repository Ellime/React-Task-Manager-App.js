import React from "react";
import classnames from "classnames";

/**
 * Used to anchor a `Menu` component to another element.
 */
const MenuAnchor = ({ className, children, ...other }) => (
  <div className={classnames("mdc-menu-anchor", className)} {...other}>
    {children}
  </div>
);

export default MenuAnchor;
