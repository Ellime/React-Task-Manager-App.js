import React from "react";
import classnames from "classnames";

/**
 * Use to divide items in a menu.
 */
const MenuDivider = ({ className, ...other }) => (
  <li
    className={classnames("mdc-list-divider", className)}
    role="separator"
    {...other}
  />
);

export default MenuDivider;
