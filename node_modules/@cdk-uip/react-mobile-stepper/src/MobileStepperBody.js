import React from "react";
import classnames from "classnames";

/**
 * Use this to render custom content between the stepper header
 * and footer.
 */
const MobileStepperBody = ({ className, ...other }) => (
  <div
    className={classnames("cdk-mobile-stepper__body", className)}
    {...other}
  />
);

export default MobileStepperBody;
