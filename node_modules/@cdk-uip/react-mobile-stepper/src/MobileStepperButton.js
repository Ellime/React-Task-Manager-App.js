import React from "react";
import { Button } from "@cdk-uip/react-button";

/**
 * Use with Mobile Steppers for the Back and Next button.
 */
const MobileStepperButton = ({ className, ...other }) => (
  <Button className={className} {...other} />
);

export default MobileStepperButton;
