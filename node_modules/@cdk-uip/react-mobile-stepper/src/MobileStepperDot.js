import React from "react";
import classnames from "classnames";

const MobileStepperDot = ({ active }) => (
  <div
    className={classnames("cdk-mobile-stepper__dot", {
      "cdk-mobile-stepper__dot--active": active
    })}
  />
);

export default MobileStepperDot;
