import React from "react";
import classnames from "classnames";

/**
 * Used to contain the stepper buttons and progress indicator.
 */
const MobileStepperFooter = ({ className, ...other }) => (
  <footer
    className={classnames("cdk-mobile-stepper__footer", className)}
    {...other}
  />
);

export default MobileStepperFooter;
