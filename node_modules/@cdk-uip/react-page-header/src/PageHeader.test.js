import React from "react";
import { PageHeader } from ".";
import renderer from "react-test-renderer";

it("renders ", () => {
  expect(
    renderer.create(<PageHeader>page header</PageHeader>)
  ).toMatchSnapshot();
});
