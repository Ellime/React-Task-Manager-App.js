import React from "react";
import { PageSpinner } from ".";
import { storiesOf } from "@storybook/react";

storiesOf("page-spinner", module)
  .add("Show Spinner", () => <PageSpinner show />)
  .add("Spinner Hidden", () => <PageSpinner />);
