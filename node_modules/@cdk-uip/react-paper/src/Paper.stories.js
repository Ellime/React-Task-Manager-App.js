import React from "react";
import { Paper } from ".";
import { storiesOf } from "@storybook/react";

const Background = ({ children }) => (
  <div style={{ backgroundColor: "#f5f5f5", padding: 20 }}>{children}</div>
);

storiesOf("paper", module).add("with surrounding background", () => (
  <Background>
    <Paper>
      <p>Paper</p>
    </Paper>
  </Background>
));
