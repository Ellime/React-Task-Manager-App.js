import React from "react";
import { Snackbar } from ".";
import renderer from "react-test-renderer";

// jest.useFakeTimers();

it("renders", () => {
  expect(
    renderer.create(
      <Snackbar show message="Hello" actionText="Action" onAction={() => {}} />
    )
  ).toMatchSnapshot();
});
