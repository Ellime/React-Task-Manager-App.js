import React from "react";
import { SpeedDial, SpeedDialItem } from ".";
import { storiesOf } from "@storybook/react";
import { action } from "@storybook/addon-actions";

storiesOf("speed-dial", module).add("closed", () => (
  <div>
    <div>Content</div>
    <SpeedDial icon="home">
      <SpeedDialItem icon="email" onClick={action("email")} />
      <SpeedDialItem icon="chat" onClick={action("chat")} />
      <SpeedDialItem icon="call" onClick={action("call")} />
    </SpeedDial>
  </div>
));
