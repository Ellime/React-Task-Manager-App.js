import React from "react";
import classnames from "classnames";

/**
 * Used to separate horizontal steps.
 */
const StepDivider = ({ className, ...other }) => (
  <li className={classnames("cdk-stepper__divider")} {...other} />
);

export default StepDivider;
