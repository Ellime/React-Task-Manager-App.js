import React from "react";
import classnames from "classnames";

const TabText = ({ className, children, ...other }) => (
  <span className={classnames("mdc-tab__icon-text", className)} {...other}>
    {children}
  </span>
);

export default TabText;
