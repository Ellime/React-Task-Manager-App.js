import React from "react";
import classnames from "classnames";

const ToolbarSpacer = ({ className, ...other }) => (
  <div className={classnames("cdk-toolbar--spacer", className)} {...other} />
);

export default ToolbarSpacer;
