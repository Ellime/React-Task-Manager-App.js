import React from "react";
import classnames from "classnames";

/**
 * The toolbar title.
 */
const ToolbarTitle = ({ className, children }) => (
  <span className={classnames("mdc-toolbar__title", className)}>
    {children}
  </span>
);

export default ToolbarTitle;
