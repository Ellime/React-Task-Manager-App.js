import React from "react";
import { Icon } from "@cdk-uip/react-icon";

const TopAppBarIcon = ({ className, children, ...other }) => (
  <span className={className}>
    <Icon aria-hidden="true" {...other}>
      {children}
    </Icon>
  </span>
);

export default TopAppBarIcon;
