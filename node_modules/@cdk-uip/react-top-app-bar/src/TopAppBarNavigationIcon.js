import React from "react";
import classnames from "classnames";
import TopAppBarIcon from "./TopAppBarIcon";

/**
 * A navigation icon that appears at the start of the app bar.
 */
const TopAppBarNavigationIcon = ({ className, ...other }) => (
  <TopAppBarIcon
    className={classnames("mdc-top-app-bar__navigation-icon", className)}
    {...other}
  />
);

export default TopAppBarNavigationIcon;
