import React from "react";
import classnames from "classnames";

/**
 * A row in the top app bar. Use this to wrap `<TopAppBarSection>` components.
 */
const TopAppBarRow = ({ className, ...other }) => (
  <div className={classnames("mdc-top-app-bar__row", className)} {...other} />
);

export default TopAppBarRow;
