import React from "react";
import classnames from "classnames";

/**
 * Your application title.
 */
const TopAppBarTitle = ({ className, ...other }) => (
  <span
    className={classnames("mdc-top-app-bar__title", className)}
    {...other}
  />
);

export default TopAppBarTitle;
