import React from "react";

// Legacy typography support
const Subheading1 = ({ adjustMargin, children, ...other }) => (
  <h4
    style={{
      fontSize: ".938rem",
      lineHeight: "1.5rem",
      fontWeight: 400,
      marginTop: adjustMargin ? 0 : undefined
    }}
    {...other}
  >
    {children}
  </h4>
);

export default Subheading1;
