import React from "react";

// Legacy typography support
const Subheading2 = ({ adjustMargin, children, ...other }) => (
  <h3
    style={{
      fontSize: "1rem",
      lineHeight: "1.75rem",
      fontWeight: 400,
      marginTop: adjustMargin ? 0 : undefined
    }}
    {...other}
  >
    {children}
  </h3>
);

export default Subheading2;
