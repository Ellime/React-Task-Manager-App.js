import React from "react";

// Legacy typography support
const Title = ({ adjustMargin, children, ...other }) => (
  <h2
    style={{
      fontSize: "1.25rem",
      lineHeight: "2rem",
      fontWeight: 500,
      marginTop: adjustMargin ? 0 : undefined
    }}
    {...other}
  >
    {children}
  </h2>
);

export default Title;
