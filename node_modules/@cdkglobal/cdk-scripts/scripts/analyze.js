'use strict';

var npmbin = require('./utils/npmbin');

var args = ['build/static/js/main.*'];
npmbin('source-map-explorer', args, function(code) {
  if (code !== 0) {
    console.error('`source-map-explorer ' + args.join(' ') + '` failed');
    process.exit(1);
  }
});
