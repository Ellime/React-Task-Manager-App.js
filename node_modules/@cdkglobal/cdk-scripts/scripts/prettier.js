'use strict';

var npmbin = require('./utils/npmbin');

var args = ['--write', 'src/**/*.{js,css}'];
npmbin('prettier', args, function(code) {
  if (code !== 0) {
    console.error('`prettier ' + args.join(' ') + '` failed');
    process.exit(1);
  }
});
